; Allow native module install scripts (better-sqlite3, argon2, esbuild) so that
; `npm install` builds/prebuilds binaries for the current Node ABI without a
; manual `npm rebuild`. Restores the default npm behavior that the allowScripts
; safety feature otherwise blocks.
allow-scripts=true
